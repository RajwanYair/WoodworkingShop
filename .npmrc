# Inherited from MyScripts/.tools/.npmrc
# Enforce Node engine declared in package.json to prevent silent upgrades.
engine-strict=true

# Lock to exact installed versions for reproducible installs.
save-exact=true

fund=false
audit-level=high
