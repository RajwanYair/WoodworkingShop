/**
 * Phase 12 / Sprint 8 — Engine validation sub-module barrel.
 * Covers: cabinet config validation rule engine.
 *
 * @example
 * ```ts
 * import { validateConfig } from '../engine/validation';
 * ```
 */
export { validateConfig } from '../validation.ts';
